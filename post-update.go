package main

import (
/*"fmt"
"github.com/gitDashboard/client"
"os"*/
)

func main() {
	/*gitDir := os.Getenv("GIT_DIR")
	if gitDir == "" {
		fmt.Errorf("Error: Error GIT_DIR env not found\n")
		os.Exit(1)
	}
	js, err := client.GenerateGraph(gitDir)
	if err != nil {
		fmt.Errorf("Error:%s\n", err.Error())
		os.Exit(1)
	}
	fmt.Println(js)
	os.Exit(0)*/
}
